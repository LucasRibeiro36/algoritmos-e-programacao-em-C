//* Variaveis em C *//
#include <stdio.h>

int main() {
    int soma, a, b;
    a = 1;
    b = 4;
    soma = a + b;
    printf("a soma de %d + %d = %d\n", a, b, soma);
    a = 5;
    b = 3;
    soma = a + b;
    printf("a soma de %d + %d = %d\n", a, b, soma);
}
